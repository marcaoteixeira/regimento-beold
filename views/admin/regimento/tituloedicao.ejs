<%- include('../../partials/header.ejs') %>
<%- include('../../partials/navbar.ejs') %>
    <hr>
    <div class="container">
        <div class="card-header">
            <h2>Titulos - Edição</h2>
        </div>
        
            <table class="table table-bordered">
                           
                <tbody>            
                    <% titulo.forEach((titulo)=> { %>
                        <tr>
                            <td>
                                <%= titulo.id %>
                            </td>
                            <td>
                                <%= titulo.titulo %> 
                            </td>
                            <td> 
                            
                                <form method="post" action="/admin/titulo/update">
                                <input type="hidden" name="id" value="<%= titulo.id %>">
                                <button class="btn btn-success">Editar</button>
                            </form>

                            </td>
                            <td>
                                <form method="post" action="/admin/titulo/delete" style="display: inline;" onsubmit="confirmarDelecao(event, this)">
                                    <input type="hidden" name="id" value="<%= titulo.id %>">
                                    <button class="btn btn-danger">Excluir</button>
                                </form>
                        
                        
                            </td>
                        </tr>
                        <% }) %>
                </tbody>
            
            </table>
               
    </div>
<%- include('../../partials/footer.ejs') %>
<script>
    function confirmarDelecao(event, form){
        event.preventDefault();
        console.log("O formulário não foi enviado!");
        var decision = confirm("Você quer realmente deletar esse Título?")
        if(decision){
            console.log("Título Excluido!")
            form.submit();
        }else{
            console.log("Não Deletado!")
        }
    }
</script>