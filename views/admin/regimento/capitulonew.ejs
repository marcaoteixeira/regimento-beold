<%- include('../../partials/header.ejs') %>
<%- include('../../partials/navbar.ejs') %>
<div class="container">
    <hr>
    <div class="card">
        <div class="card-body">
            <div class="card-header">
                <h2>Adicionar Capítulo</h2>
            </div>

            <form method="post" action="/admin/capitulo/new">
                Titulo:
                <select name="id_titulo" class="form-control">
                    <% titulo.forEach((titulo)=> { %>
                        <option value="<%= titulo.id %>" title="<%= titulo.id %>">
                            <%= titulo.titulo %>
                        </option>
                        <% }) %>
                </select>
                Capitulo:<input class="form-control" type="text" name="capitulo" placeholder="Capitulo"><br>
                               

                <button class="btn btn-success">Cadastrar</button>
            </form>
        </div>
    </div>


</div>
        
<%- include('../../partials/footer.ejs') %>