<%- include('../../partials/header.ejs') %>
<%- include('../../partials/navbar.ejs') %>
    <hr>
    <div class="container">
        <div class="card-header">
            <h2>Comissões - Edição</h2>
        </div>
        
            <table class="table table-bordered">
                           
                <tbody>            
                    <% comissoes.forEach((comissao)=> { %>
                        <tr>
                            <td>
                                <%= comissao.id %>
                            </td>
                            <td>
                                <%= comissao.comissao %> - <%= comissao.sigla %>
                            </td>
                            <td> 
                            
                                <form method="post" action="/admin/comissao/update">
                                <input type="hidden" name="comissao" value="<%= comissao.id %>">
                                <button class="btn btn-success">Editar</button>
                            </form>

                            </td>
                            <td>
                                <form method="post" action="/admin/comissao/delete" style="display: inline;" onsubmit="confirmarDelecao(event, this)">
                                    <input type="hidden" name="comissao" value="<%= comissao.id %>">
                                    <button class="btn btn-danger">Excluir</button>
                                </form>
                        
                        
                            </td>
                        </tr>
                        <% }) %>
                </tbody>
            
            </table>
               
    </div>
<%- include('../../partials/footer.ejs') %>
<script>
    function confirmarDelecao(event, form){
        event.preventDefault();
        console.log("O formulário não foi enviado!");
        var decision = confirm("Você quer realmente deletar essa Comissão?")
        if(decision){
            console.log("Comissão excluida!")
            form.submit();
        }else{
            console.log("Não Deletada!")
        }
    }
</script>